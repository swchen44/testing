# Claude Code 運作原理

> 給不熟悉技術的朋友看的簡單說明

---

## 一句話解釋

**你用中文（或任何語言）說出你想做的事，Claude Code 幫你把想法變成真正可以執行的程式或文件。**

---

## 整體流程圖

```mermaid
flowchart TD
    A([你輸入指令\n例如：「幫我整理這份 PDF」]) --> B[Claude 理解你的意圖]

    B --> C{需要哪些工具？}

    C -->|讀取檔案| D[Read 工具\n讀取本機檔案]
    C -->|搜尋內容| E[Grep 工具\n在程式碼中搜尋關鍵字]
    C -->|新增/修改檔案| F[Write / Edit 工具\n建立或修改檔案]
    C -->|執行指令| G[Bash 工具\n執行系統指令]
    C -->|查詢網路| H[WebSearch 工具\n搜尋最新資訊]
    C -->|上傳程式碼| I[Git 工具\n提交並推送到 GitHub]

    D & E & F & G & H & I --> J[整合所有工具的結果]

    J --> K[Claude 產生回應\n或完成任務]

    K --> L{任務完成了嗎？}

    L -- 是 --> M([回報給你，任務結束])
    L -- 否 --> B
```

---

## 各工具是做什麼的？

| 工具名稱 | 功能說明 | 生活化比喻 |
|----------|----------|------------|
| `Read` | 讀取你電腦上的檔案 | 像翻開一本書 |
| `Write` | 建立新的檔案 | 像拿出一張白紙寫字 |
| `Edit` | 修改已有的檔案 | 像用修正液改作業 |
| `Grep` | 在大量檔案中搜尋關鍵字 | 像用 Ctrl+F 全文搜尋 |
| `Glob` | 找出符合條件的檔案 | 像用篩子過濾檔案 |
| `Bash` | 執行系統指令（安裝軟體、跑測試等） | 像打開終端機輸入指令 |
| `WebSearch` | 搜尋網路上的最新資訊 | 像 Google 搜尋 |
| `Git` | 把程式碼存檔並上傳到 GitHub | 像把報告存檔並寄出去 |
| `Agent` | 派出子任務給專門的小助手 | 像主管把工作分派給專員 |
| `TodoWrite` | 記錄並管理待辦事項 | 像便利貼清單 |

---

## 更詳細的互動流程

```mermaid
sequenceDiagram
    participant 你 as 你（使用者）
    participant Claude as Claude AI 大腦
    participant 工具 as 各種工具
    participant 系統 as 你的電腦 / GitHub

    你->>Claude: 輸入指令（自然語言）
    Claude->>Claude: 分析意圖，規劃步驟

    loop 反覆執行直到完成
        Claude->>工具: 呼叫適合的工具
        工具->>系統: 對電腦/網路執行操作
        系統-->>工具: 回傳結果
        工具-->>Claude: 結果回報給 Claude
        Claude->>Claude: 判斷下一步
    end

    Claude->>你: 任務完成，回報結果
```

---

## 安全機制

```mermaid
flowchart LR
    A[Claude 想執行某個動作] --> B{這個動作安全嗎？}

    B -->|低風險\n例如：讀取檔案| C[自動執行]
    B -->|中風險\n例如：修改檔案| D[詢問你是否允許]
    B -->|高風險\n例如：強制推送、刪除資料| E[警告並等待你明確確認]

    D --> F{你的回應}
    E --> F

    F -- 允許 --> C
    F -- 拒絕 --> G[停止，換個方式]
```

---

## 簡單比喻

把 Claude Code 想像成一位**超強的工程師助理**：

1. **你說話（自然語言）** → 他聽懂你的需求
2. **他去查資料、讀程式碼** → 了解現況
3. **他動手做修改** → 幫你完成工作
4. **他把成果交給你** → 報告結果
5. **危險動作會先問你** → 不會擅自做重大決定

---

*由 Claude Code 自動生成 | 2026-03-16*
